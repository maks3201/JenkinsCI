#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh ubuntu@ub1 \'hostname\''
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh ubuntu@ub1 \'uptime\''
            }
        }
    }
}
